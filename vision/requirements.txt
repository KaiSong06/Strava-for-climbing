fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
# Image processing
opencv-python-headless==4.9.0.80
numpy==1.26.4
Pillow==10.3.0
# ML models — loaded once at startup, never per-request
torch==2.3.0
torchvision==0.18.0
timm
transformers==4.41.1
# SAM — install from source (no stable PyPI release)
segment-anything @ git+https://github.com/facebookresearch/segment-anything.git
# Job queue (Redis)
redis==5.0.4
# 3D mesh generation (GLB export)
trimesh>=4.0.0
# Utilities
python-dotenv==1.0.1
pydantic==2.7.1
httpx==0.27.0
# Structured logging / error tracking
sentry-sdk[fastapi]==2.18.0
